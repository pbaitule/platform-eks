apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: test-cluster-t2-micro
  region: us-east-1

nodeGroups:
  - name: nodeGroup1
    instanceType: t2.small
    desiredCapacity: 2
    minSize: 2
    maxSize: 2
    ssh:
      publicKeyPath: ~/.ssh/pbaitule_aws.pub
